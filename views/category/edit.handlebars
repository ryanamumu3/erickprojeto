<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Editar Categoria</title>
</head>
<body>
  <h1>Editar Categoria</h1>
  <form action="/categories/edit/{{category.id}}" method="POST">
    <label for="nome">Nome:</label>
    <input type="text" id="nome" name="nome" value="{{category.nome}}" required>
    <br>
    <label for="descricao">Descrição:</label>
    <textarea id="descricao" name="descricao">{{category.descricao}}</textarea>
    <br>
    <button type="submit">Salvar</button>
  </form>
</body>
</html>
